PROJECT_NAME="Enterprise Cybersecurity Platform"
API_V1_STR="/api/v1"

# PostgreSQL
DATABASE_URL="postgresql+asyncpg://user:password@host:port/db_name"

# Redis
REDIS_HOST="localhost"
REDIS_PORT="6379"

# JWT
JWT_SECRET_KEY="your_strong_secret_key_here"
JWT_ALGORITHM="HS256"
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# For multi-device MikroTik configuration
# Generate a strong Fernet key using: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())
DEVICE_CREDENTIAL_ENCRYPTION_KEY="your_fernet_encryption_key_here"